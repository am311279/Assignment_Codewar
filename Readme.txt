This repository contains four simple Python functions that demonstrate basic programming concepts such as conditionals, loops, and string manipulation.

Each function performs a specific, commonly needed task — such as checking if a number is even, converting numbers to strings, removing spaces from text, and counting vowels in a sentence.

📂 Project Overview
Function Name	Description
even_or_odd(number)	Determines whether a given number is even or odd.

number_to_string(num)	Converts a number to its string representation.

no_space(x)	Removes all spaces from a given string.
get_count(sentence)	Counts the number of vowels (a, e, i, o, u) in a string.

Requirements
Python 
No external libraries required

How to Run
Clone or download this repository.
Open the file in your code editor (e.g., VS Code, PyCharm, etc.).
Run the program using:
python filename.py

🧩 Function Details
1️⃣ even_or_odd(number)

Description:
Checks if a given integer is even or odd.

Example:

print(even_or_odd(4))   # Output: "Even"
print(even_or_odd(7))   # Output: "Odd"

2️⃣ number_to_string(num)

Description:
Converts an integer or float to a string.

Example:

print(number_to_string(123))   # Output: "123"
print(number_to_string(9.87))  # Output: "9.87"

3️⃣ no_space(x)

Description:
Removes all spaces from a given string.

Example:

print(no_space("Hello World"))      # Output: "HelloWorld"
print(no_space("Python  is  fun"))  # Output: "Pythonisfun"

4️⃣ get_count(sentence)

Description:
Counts how many vowels are present in the input string.

Example:

print(get_count("hello"))        # Output: 2
print(get_count("programming"))  # Output: 3


Author
Aung Myat
aungmyat1@gmail.com
